<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.merka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".EditProdutoLoja"></activity>
        <activity android:name=".AdicionaProduto" />
        <activity android:name=".ProdutosLoja" />
        <activity android:name=".EditLojaPerfil" />
        <activity android:name=".PerfilLoja" />
        <activity android:name=".CriarLoja" />
        <activity android:name=".RedefinirSenha" />
        <activity android:name=".EditPerfil" />
        <activity android:name=".Perfil" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tela_Inicial" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>